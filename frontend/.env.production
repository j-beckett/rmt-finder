# Production builds fetch the API from the same origin that served the page
# (uvicorn serves dist/ directly — see mount_frontend in backend/api.py), so
# the base URL is deliberately empty. Dev keeps the localhost:8000 default.
VITE_API_BASE_URL=
